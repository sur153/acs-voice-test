# Production Environment Variables
# Used when building for deployment to the backend

# WebSocket URL - relative path for same-origin
VITE_WS_URL=/web/ws

# Disable mock service in production
VITE_USE_MOCK=false
